{
  "name": "kards-deck-image-api",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    // 安装依赖后，显式执行安装命令
    "postinstall": "npx puppeteer browsers install chrome"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^21.0.0"
    // 注意：如果使用 puppeteer-core，需要额外安装 chrome
  },
  "engines": {
    "node": ">=18.0.0"
  }
}p